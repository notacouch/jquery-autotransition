( function( $ ) {

	"use strict";

	// create an element to run tests inside
	var $testCanvas = $( "<div id='testCanvas' style='position: relative;'></div>" );

	$( "body" ).prepend( $testCanvas );

}( jQuery ) );
